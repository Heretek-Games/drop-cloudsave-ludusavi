{
  "name": "drop-cloudsave-ludusavi",
  "version": "0.1.0",
  "description": "Ludusavi-backed cloud save path resolution plugin.",
  "type": "module",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "node --test dist/test/*.js",
    "typecheck": "tsc --noEmit",
    "sign": "drop-plugin sign ."
  },
  "dependencies": {
    "@droposs/plugin-sdk": "^0.4.0",
    "yaml": "^2.9.1"
  },
  "devDependencies": {
    "@droposs/plugin-cli": "^0.4.0",
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "license": "MIT"
}